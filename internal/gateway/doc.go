// Package gateway is the gateway simulator: the Basic Station LNS client, the
// uplink-parsing bridge that turns tag PHYPayloads into jreq/updf messages, the
// channel plan, and (in later commits) the TCP server, conn registry, and RX
// windows. EUI-64 identity helpers live in the leaf package internal/euid.
package gateway
